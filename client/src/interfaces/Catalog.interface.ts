export interface RootState {
    state: any;
    user: (string | any);
}
export interface IProps_Square {
    message: string;
    onClick: React.MouseEventHandler<HTMLButtonElement>;
    id: (number | string | any);
}
export interface StateData {
    name: string;
    category: string;
    value: string;
}
